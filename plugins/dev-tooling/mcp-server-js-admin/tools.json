{
  "tools": [
    {
      "name": "eslint_check",
      "description": "ESLint code quality check (dry-run). Returns violations. Runs in Administration context.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "paths": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "File paths or directories to lint. When supplied, these are the ONLY targets the run covers; repo-root-relative paths are rebased onto the Administration package directory. Omit to lint the targets the aggregate lint script configures for itself."
          },
          "output_format": {
            "type": "string",
            "enum": [
              "stylish",
              "json"
            ],
            "default": "stylish",
            "description": "ESLint output format"
          },
          "scope": {
            "type": "string",
            "description": "Scope name from .mcp-js-tooling.json. Omit to use the configured default_scope. Pass \"shopware\" to force project-root behavior."
          }
        }
      }
    },
    {
      "name": "eslint_fix",
      "description": "ESLint auto-fix. Applies code style fixes in Administration context.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "paths": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "File paths or directories to fix. When supplied, these are the ONLY targets the run covers and the only files that can be modified; repo-root-relative paths are rebased onto the Administration package directory. Omit to fix the targets the aggregate lint script configures for itself."
          },
          "scope": {
            "type": "string",
            "description": "Scope name from .mcp-js-tooling.json. Omit to use the configured default_scope. Pass \"shopware\" to force project-root behavior."
          }
        }
      }
    },
    {
      "name": "stylelint_check",
      "description": "Stylelint SCSS/CSS check (dry-run). Returns violations in Administration context.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "paths": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "File paths or glob patterns, relative to the Administration package directory. When supplied, these are the ONLY targets the run covers. Omit to lint the targets the aggregate SCSS lint script configures for itself."
          },
          "output_format": {
            "type": "string",
            "enum": [
              "string",
              "json",
              "compact"
            ],
            "default": "string",
            "description": "Stylelint output format"
          },
          "scope": {
            "type": "string",
            "description": "Scope name from .mcp-js-tooling.json. Omit to use the configured default_scope. Pass \"shopware\" to force project-root behavior."
          }
        }
      }
    },
    {
      "name": "stylelint_fix",
      "description": "Stylelint auto-fix. Applies style fixes in Administration context.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "paths": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "File paths or glob patterns, relative to the Administration package directory. When supplied, these are the ONLY targets the run covers and the only files that can be modified. Omit to fix the targets the aggregate SCSS fix script configures for itself."
          },
          "scope": {
            "type": "string",
            "description": "Scope name from .mcp-js-tooling.json. Omit to use the configured default_scope. Pass \"shopware\" to force project-root behavior."
          }
        }
      }
    },
    {
      "name": "prettier_check",
      "description": "Prettier format check (dry-run). Reports files whose formatting differs from the project config.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "paths": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "File paths or glob patterns, relative to the Administration package directory. When supplied, these are the ONLY targets the run covers. Omit to check the targets the aggregate format script configures for itself."
          },
          "scope": {
            "type": "string",
            "description": "Scope name from .mcp-js-tooling.json. Omit to use the configured default_scope. Pass \"shopware\" to force project-root behavior."
          }
        }
      }
    },
    {
      "name": "prettier_fix",
      "description": "Prettier auto-format. Rewrites files to match the project config.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "paths": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "File paths or glob patterns, relative to the Administration package directory. When supplied, these are the ONLY targets the run covers and the only files that can be modified. Omit to format the targets the aggregate format script configures for itself."
          },
          "scope": {
            "type": "string",
            "description": "Scope name from .mcp-js-tooling.json. Omit to use the configured default_scope. Pass \"shopware\" to force project-root behavior."
          }
        }
      }
    },
    {
      "name": "jest_run",
      "description": "Jest test runner for Administration. Returns test results.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "testPathPatterns": {
            "type": "string",
            "description": "Regex pattern for test file paths (--testPathPatterns)"
          },
          "testNamePattern": {
            "type": "string",
            "description": "Regex pattern for test names (--testNamePattern)"
          },
          "coverage": {
            "type": "boolean",
            "default": false,
            "description": "Generate code coverage report"
          },
          "updateSnapshots": {
            "type": "boolean",
            "default": false,
            "description": "Update Jest snapshots (--updateSnapshot)"
          },
          "ci": {
            "type": "boolean",
            "default": false,
            "description": "Run Jest in CI mode (--ci). Defaults to false. Turning it on makes the Jest config collect coverage regardless of the \"coverage\" argument and swaps the reporters for jest-silent-reporter plus jest-junit, which suppresses the per-test lines and the summary. Leave it off unless you need CI-identical behavior."
          },
          "scope": {
            "type": "string",
            "description": "Scope name from .mcp-js-tooling.json. Omit to use the configured default_scope. Pass \"shopware\" to force project-root behavior."
          }
        }
      }
    },
    {
      "name": "tsc_check",
      "description": "TypeScript type checking. Runs 'npm run lint:types' which uses project tsconfig.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "scope": {
            "type": "string",
            "description": "Scope name from .mcp-js-tooling.json. Omit to use the configured default_scope. Pass \"shopware\" to force project-root behavior."
          }
        }
      }
    },
    {
      "name": "lint_all",
      "description": "Run ALL lint checks (TypeScript, ESLint, Stylelint, Prettier) in one command. Returns all violations.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "scope": {
            "type": "string",
            "description": "Scope name from .mcp-js-tooling.json. Omit to use the configured default_scope. Pass \"shopware\" to force project-root behavior."
          }
        }
      }
    },
    {
      "name": "lint_twig",
      "description": "ESLint check for Twig templates (.html.twig files). Validates Admin Vue component templates.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "scope": {
            "type": "string",
            "description": "Scope name from .mcp-js-tooling.json. Omit to use the configured default_scope. Pass \"shopware\" to force project-root behavior."
          }
        }
      }
    },
    {
      "name": "unit_setup",
      "description": "Regenerate component import resolver map. Run this when tests fail with import/module resolution errors.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "scope": {
            "type": "string",
            "description": "Scope name from .mcp-js-tooling.json. Omit to use the configured default_scope. Pass \"shopware\" to force project-root behavior."
          }
        }
      }
    },
    {
      "name": "vite_build",
      "description": "Vite build for Administration. Compiles Vue 3 assets.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "mode": {
            "type": "string",
            "enum": [
              "development",
              "production"
            ],
            "default": "production",
            "description": "Build mode"
          },
          "scope": {
            "type": "string",
            "description": "Scope name from .mcp-js-tooling.json. Omit to use the configured default_scope. Pass \"shopware\" to force project-root behavior."
          }
        }
      }
    }
  ]
}
